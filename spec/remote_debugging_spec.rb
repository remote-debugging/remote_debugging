require 'spec_helper'

describe RemoteDebugging do
  it 'has a version number' do
    expect(RemoteDebugging::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
