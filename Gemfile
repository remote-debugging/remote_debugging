source 'https://rubygems.org'

# Specify your gem's dependencies in remote_debugging.gemspec
gemspec

# todo: remote this line, once we test our hack in a real app
gem 'remote_debugging-pry', path: '../remote_debugging-pry'
